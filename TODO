# TODO


Call_Log ver 3.2 TODO
===================

   Web interface

1. Add duration of call in history (i.e. Call start, call answered, call ended)
2. Multicurl to get caller info and hangup at the same time.
3. Create SaaS using syslog function
4. Send to Loggly
5. Automatically record calls
6. Export call history to CSV
7. Integration with OpenCNAM
8. Admin Web interface to view the calls, add caller id info and analytics
9. Check robocall and SPAM score with Youmail
10. Database connectivity
11. Redis queue/memcache functionality
12. Twilio integration
13. Look into append mode for file_put_contents
14. Queue spam lookup for new calls
15. Check if file exists for blacklists and whitelists
16. Performance: Use cookies and don't reauthenticate (check headers)
17. Use Cloudflare Javascript workers to combine API responses (with cache)
18. Use user login instead of admin login
19. Use star codes to block Caller ID
20. Default instructions *77 to block anonymous callers
21. Use Blind Transfer or dual ringing to block callers at SaaS (or digit maps)
22. Use digit maps to white list SaaS if performing call blocking there
23. Integration: Google Hub to display caller info
24. Scrape and update InboundCallRoute for blocking
25. Use custom firmware to increase InboundCallRoute fields length
26. Just poll current caller instead of hook status (NOTE: OFF HOOK happens first)
27. Performance: "No current callers" needs to be while loop until there are current caller
28. Send Email (Mailgun)
29. Encrypt Pushbullet